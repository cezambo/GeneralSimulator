{
  "name": "@sim/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Núcleo headless: mundo, substrato, corpo, cognição, Validador e camada de LLM. X-001.",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "vite-node src/cli/run.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/ws": "^8.18.1"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "ws": "^8.21.1",
    "yaml": "^2.9.0"
  }
}
